const AUTH_ROUTES = {
    login: '/login',
    signup: '/signup',
  };
  
  const PROTECTED_ROUTES_NAMES = {
    root: '/',
    increment: '/increment',
    todolist: '/todolist',
  };
  
  export const ROUTES = { AUTH_ROUTES, PROTECTED_ROUTES_NAMES };
  